include logos-vars

mixin list(imgSrcList)
    .logos-item
        a(href="#")
            img.img-responsive(src=imgSrcList, alt='logos')

- for(var x = 0; x < 5; x++){
    +list(imgSrcList[x])
- }






